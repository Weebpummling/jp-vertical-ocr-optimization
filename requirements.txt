# Runtime dependencies for the reading layer (structure registration).
#
# Headless OpenCV: the pipeline never opens a window, and the GUI build drags in
# system libraries CI does not have.
opencv-python-headless>=4.8
numpy>=1.26

# Workstation API (app/). Only api.py imports these; page_service.py stays
# framework-free so its logic is testable without a server.
fastapi>=0.115
uvicorn>=0.30

# The database itself needs nothing: it is a SQLite file, and sqlite3 ships with
# Python. scripts/migrate_from_postgres.py is the one exception and is one-shot -
# `pip install "psycopg[binary]"` if you ever need to re-run it.
